export const userState = (state) => state.user;
export const errorState = (state) => state.error_reducer;
export const pageState = (state) => state.page;
export const arrayItemState = (state) => state.allArticles;